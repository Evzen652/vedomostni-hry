# Claude Code lokální nastavení (per-stroj, neverzovat)
.claude/settings.local.json

# závislosti (kdyby se v budoucnu přidaly)
node_modules/

# OS smetí
.DS_Store
Thumbs.db

# Cloudflare / online režim
# (pozor: .gitignore neumí komentář za vzorem, musí být na vlastním řádku)
# lokální stav D1 a cache wrangleru
.wrangler/
# generuje se z data/questions/*.json přes npm run db:seed
data/d1-seed.sql
# lokální tajemství, nikdy neverzovat
.dev.vars

# sestavuje se z korene pres npm run build (viz scripts/build-public.js)
dist/
